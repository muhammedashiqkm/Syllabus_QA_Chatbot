# docker-compose.yml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    depends_on:
      - memcached

  memcached:
    image: memcached:1.6-alpine
    restart: always